%h1 Listing assets

%table
  %tr
    %th Policy area
    %th Sub area
    %th Topic
    %th Title
    %th Year
    %th State
    %th Format
    %th Level
    %th Type
    %th
    %th
    %th

  - @assets.each do |asset|
    %tr
      %td= asset.policy_area
      %td= asset.sub_area
      %td= asset.topic
      %td= asset.title
      %td= asset.year
      %td= asset.state
      %td= asset.format
      %td= asset.level
      %td= asset.type_of
      %td= link_to 'Show', asset
      %td= link_to 'Edit', edit_asset_path(asset)
      %td= link_to 'Destroy', asset, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Asset', new_asset_path
